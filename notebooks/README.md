# Notebook organization

The notebooks are copied from the connected Drive resources and renamed only to
make their pipeline role clear. Original Drive IDs and modification times are in
`artifacts/drive_source_manifest.json`.

`notebooks/exports/` contains searchable script-style exports. These exports
comment notebook magics but may still depend on cell order and notebook globals.
Use the `.ipynb` files as the authoritative source.
